BINARY          = gaia

DEBUG           = -g -O

INCLUDE         = -Iinclude/

LIBS            = -Llib -Wl,--no-as-needed -llua -ldl -lSDL2 -lGL -lGLEW -lpthread

CXXFLAGS        = -Wall -std=c++11 -Wcomment $(DEBUG)
CPPFLAGS        = $(INCLUDE) -I.
